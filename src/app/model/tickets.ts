export class Tickets{
    id:string;
    number:string;
    title:string;
    exists:string;

    constructor(id:string, number:string, title:string, exists:string){
        this.id = id;
        this.number = number;
        this.title = title;
        this.exists = exists;
    }

} 